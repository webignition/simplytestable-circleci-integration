machine:
  php:
    version: 5.5.36

dependencies:
  override:
    - git clone git@github.com:webignition/worker.simplytestable.com.git
#    make proper app/config/config.yml
#    make proper app/config/parameters.yml
#    make proper ???
#    - composer install:
#        pwd: worker.simplytestable.com
    - ls -la
    - mysql -u ubuntu < foo.sql

test:
  override:
    - echo foo
