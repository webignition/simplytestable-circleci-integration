machine:
  php:
    version: 5.5.36

dependencies:
  override:
    - git clone git@github.com:webignition/worker.simplytestable.com.git
    - composer install:
        pwd: worker.simplytestable.com
    - ls -la

test:
  override:
    - echo foo
